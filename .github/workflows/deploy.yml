name: deploy

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'data/**'
      - 'public/**'
      - 'astro.config.mjs'
      - 'tailwind.config.mjs'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm build
        env:
          PUBLIC_ETHICALADS_PUBLISHER: ${{ vars.PUBLIC_ETHICALADS_PUBLISHER }}

      - name: rsync to hetzner
        # GitHub restricts the ``secrets`` context inside step-level ``if:``
        # expressions, so we guard inside the shell instead: if the SSH key
        # secret isn't configured, log a notice and exit 0. This lets the
        # build verify cleanly even before Hetzner secrets are provisioned.
        env:
          HETZNER_HOST: ${{ secrets.HETZNER_HOST }}
          HETZNER_USER: ${{ secrets.HETZNER_USER }}
          HETZNER_PATH: ${{ secrets.HETZNER_PATH }}
          SSH_KEY: ${{ secrets.HETZNER_SSH_KEY }}
        run: |
          if [[ -z "${SSH_KEY:-}" ]]; then
            echo "::notice::HETZNER_SSH_KEY not set — skipping rsync; build artifact remains in dist/"
            exit 0
          fi
          mkdir -p ~/.ssh
          echo "$SSH_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H "$HETZNER_HOST" >> ~/.ssh/known_hosts
          rsync -avz --delete dist/ "$HETZNER_USER@$HETZNER_HOST:$HETZNER_PATH/"
